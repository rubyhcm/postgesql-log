\chapter*{Tóm tắt}

\begin{sloppypar}
Các hệ thống tài chính, ngân hàng và thương mại điện tử hiện đại đặt ra yêu cầu ngày càng cao về \textbf{Audit Trail} --- nhật ký ghi lại đầy đủ mọi thay đổi dữ liệu nhằm phục vụ tuân thủ pháp lý, phát hiện gian lận và khôi phục sau sự cố. Tuy nhiên, triển khai audit log trong môi trường \ac{oltp} write-heavy đồng thời phải giải quyết ba thách thức cốt lõi: (1) khối lượng dữ liệu tích lũy lớn (hàng triệu dòng, hàng chục GB), (2) yêu cầu overhead thấp để không ảnh hưởng đến throughput nghiệp vụ, và (3) đảm bảo tính bất biến, chống can thiệp độc lập với quyền quản trị viên.

Đề tài này thiết kế và triển khai kiến trúc Audit Log hiệu năng cao trên PostgreSQL 16, sử dụng thuần túy các cơ chế gốc của hệ quản trị cơ sở dữ liệu. Phương pháp đề xuất kết hợp: \textbf{Trigger PL/pgSQL} với \textbf{SECURITY DEFINER} để ghi log tự động không cần sửa mã ứng dụng; \textbf{JSONB} để lưu snapshot OLD/NEW theo hướng schema-less trên một bảng duy nhất; \textbf{Declarative Partitioning} theo thời gian để quản lý vòng đời dữ liệu hiệu quả; \textbf{GIN Index} để tăng tốc truy vấn nội dung JSONB; cơ chế \textbf{Immutability (WORM)} qua BEFORE trigger và dblink autonomous transaction; và chuỗi băm \textbf{SHA-256} tamper-evident để phát hiện can thiệp ở tầng file.

Thực nghiệm được tiến hành trên PostgreSQL 16.10, Ubuntu 22.04 LTS (WSL2), 4 vCPU / 8 GB RAM với dataset gồm 1 triệu bản ghi nghiệp vụ và 7,5 triệu dòng audit log (5.253 MB tổng). Kịch bản đo overhead được thực hiện trên \textit{scaling curve} 3 mức concurrency (10, 50, 80 clients). Kết quả cho thấy: overhead trigger nằm trong khoảng $[-5,8\%,\ +5,0\%]$ nhất quán qua cả 3 mức tải --- đạt tiêu chí $< 15\%$; \ac{tps} ổn định $\sim$33--36 qua mọi mức concurrency, xác nhận bottleneck là phần cứng (4 vCPU) chứ không phải trigger; \texttt{DROP\ PARTITION} giải phóng 1 triệu dòng trong 47 ms --- nhanh hơn \texttt{DELETE} truyền thống $\sim$13,6 lần; GIN cải thiện $\sim$10\% với warm cache; cả 3 kịch bản kiểm thử bảo mật đều PASS. Kết quả chứng minh tính khả thi của giải pháp thuần PostgreSQL cho bài toán audit log doanh nghiệp.

\textbf{Từ khóa:} PostgreSQL; Audit Log; JSONB; GIN Index; Partitioning; Immutability; WORM; Tamper-evident; PL/pgSQL.
\end{sloppypar}
